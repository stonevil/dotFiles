# vim:ft=sh :

# Install tmux
if ! command -v tmux >/dev/null; then
	if command -v brew >/dev/null; then
		brew install tmux
	fi
fi

function stmux() { ssh $@ -t bash -c '~/.homebrew/bin/tmux attach-session -dv'; }
