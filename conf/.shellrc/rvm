# RVM #### #####################################################################
# Add RVM to the PATH
export RVM=~/.rvm/
if [[ -d $RVM ]]; then
  export PATH=$RVM/bin:$PATH
  export MANPATH=$RVM/man:$MANPATH
	[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm"
fi
