windows:
  - name: "default"
    clear: true
    root: "~"
    layout: tiled
    panes:
      - cmd: [""]
      - cmd: ["sudo softwareupdate --install --all -v", "sudo brew update", "sudo brew upgrade", "sudo /opt/chef/embedded/bin/gem update"]
        width: 50
        focus: true
